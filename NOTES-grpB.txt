Backend:
    -Ray (server.py)
    -Raf (server.py)

Frontend:
    -Lu (HTML & CSS)
    -Ric (JS)
    -Jaime (JS)

Structure:
    server: - app routes that direct to html
            - DB code
    JS: - Ric and Jaime add here whatever you want

    HTML: -Forms
          -Bootstrap (toolbar => links to indexm showProfiles and addProfile)
    



create db:  -python
            -from server import db
            -from server import Profile
            -db.create_all()
            -when you manually add data to the db, don't forget to db.session.commit()